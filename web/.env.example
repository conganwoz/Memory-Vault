# Base URL of the Kindred Phoenix backend (same one the mobile app uses).
NEXT_PUBLIC_API_URL=http://localhost:4008

# Google sign-in (optional). Must also be in the backend's GOOGLE_CLIENT_IDS.
# NEXT_PUBLIC_GOOGLE_WEB_CLIENT_ID=xxxx.apps.googleusercontent.com
